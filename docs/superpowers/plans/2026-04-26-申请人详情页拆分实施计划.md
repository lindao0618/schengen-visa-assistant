# 申请人详情页拆分实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 在不改变 `/applicants/[id]` 页面行为的前提下，把 `ApplicantDetailClientPage.tsx` 从单体文件拆成页面壳、控制器、预览弹窗、Case 工作区和 tab 子组件，降低页面运行压力和后续维护风险。

**架构：** 采用“页面壳 + 控制器 Hook + 分块子组件”的方式增量拆分。先收口状态和副作用，再拆重逻辑弹窗和 Case 工作区，最后把基础 tab 和进度 tab 从主文件中移走，整个过程中保持现有接口和用户路径不变。

**技术栈：** Next.js 14 App Router、React Client Components、TypeScript、node:test、ESLint、现有 `/api/applicants` 与 `/api/cases` 接口

---

## 文件结构与职责

本次实施涉及的主要文件如下：

- 修改：`app/applicants/[id]/ApplicantDetailClientPage.tsx`
  - 从单体页面降成页面壳，只负责组装 tab、控制器和弹窗
- 新建：`app/applicants/[id]/detail/types.ts`
  - 承载详情页共享类型和动作分组类型
- 新建：`app/applicants/[id]/detail/use-applicant-detail-controller.ts`
  - 承载详情页状态、权限布尔值、拉取、刷新、保存和弹窗开关
- 新建：`app/applicants/[id]/detail/material-preview-dialog.tsx`
  - 承载 Excel/Word/文本/图片等文件预览与在线编辑
- 新建：`app/applicants/[id]/detail/audit-dialog.tsx`
  - 承载审核弹窗与自动修复交互
- 新建：`app/applicants/[id]/detail/cases-tab.tsx`
  - 承载 Case 列表、新建、编辑、分配和状态 UI
- 新建：`app/applicants/[id]/detail/basic-tab.tsx`
  - 承载基础资料编辑 UI
- 新建：`app/applicants/[id]/detail/materials-tab.tsx`
  - 承载材料列表、上传下载和打开预览/审核动作
- 新建：`app/applicants/[id]/detail/progress-tab.tsx`
  - 承载进度和状态相关展示
- 新建：`tests/applicant-detail-controller.test.ts`
  - 校验控制器层纯函数
- 新建：`tests/applicant-material-preview.test.ts`
  - 校验预览相关纯函数
- 新建：`tests/applicant-cases-tab.test.ts`
  - 校验 Case 选择与来源推导纯函数

实施时，不修改路由文件 `app/applicants/[id]/page.tsx` 的对外行为，也不修改自动化服务目录。

### 任务 1：抽离控制器与共享类型

**文件：**
- 新建：`app/applicants/[id]/detail/types.ts`
- 新建：`app/applicants/[id]/detail/use-applicant-detail-controller.ts`
- 新建：`tests/applicant-detail-controller.test.ts`
- 修改：`app/applicants/[id]/ApplicantDetailClientPage.tsx`

- [ ] **步骤 1：先写控制器纯函数的失败测试**

在 `tests/applicant-detail-controller.test.ts` 中先覆盖两类纯函数：默认 tab 解析，以及角色能力推导。先让测试失败，锁定行为边界。

```ts
import test from "node:test"
import assert from "node:assert/strict"

import {
  buildApplicantDetailCapabilities,
  resolveApplicantDetailTab,
} from "../app/applicants/[id]/detail/use-applicant-detail-controller"

test("resolveApplicantDetailTab 只接受四个合法 tab", () => {
  assert.equal(resolveApplicantDetailTab("basic"), "basic")
  assert.equal(resolveApplicantDetailTab("cases"), "cases")
  assert.equal(resolveApplicantDetailTab("materials"), "materials")
  assert.equal(resolveApplicantDetailTab("progress"), "progress")
  assert.equal(resolveApplicantDetailTab("unknown"), "basic")
  assert.equal(resolveApplicantDetailTab(""), "basic")
})

test("buildApplicantDetailCapabilities 正确推导只读与自动化权限", () => {
  assert.deepEqual(buildApplicantDetailCapabilities("service"), {
    isReadOnly: true,
    canEditApplicant: false,
    canAssignCase: false,
    canRunAutomation: false,
  })
  assert.deepEqual(buildApplicantDetailCapabilities("supervisor"), {
    isReadOnly: false,
    canEditApplicant: true,
    canAssignCase: true,
    canRunAutomation: true,
  })
})
```

- [ ] **步骤 2：运行测试，确认当前失败**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-detail-controller.test.js
```

预期：

- `tsc` 通过编译测试入口
- `node --test` 失败，提示 `resolveApplicantDetailTab` 或 `buildApplicantDetailCapabilities` 尚未导出

- [ ] **步骤 3：实现控制器纯函数与共享类型**

在 `app/applicants/[id]/detail/types.ts` 中定义控制器对页面壳暴露的最小表面；在 `use-applicant-detail-controller.ts` 中先落纯函数与 Hook 外壳。

```ts
import {
  canAssignCases,
  canTriggerAutomation,
  canWriteApplicants,
  isReadOnlyRole,
  normalizeAppRole,
} from "@/lib/access-control"

export type ApplicantDetailTab = "basic" | "cases" | "materials" | "progress"

export function resolveApplicantDetailTab(value: string | null | undefined): ApplicantDetailTab {
  return value === "cases" || value === "materials" || value === "progress" ? value : "basic"
}

export function buildApplicantDetailCapabilities(role: string | null | undefined) {
  const normalizedRole = normalizeAppRole(role)
  return {
    isReadOnly: isReadOnlyRole(normalizedRole),
    canEditApplicant: canWriteApplicants(normalizedRole),
    canAssignCase: canAssignCases(normalizedRole),
    canRunAutomation: canTriggerAutomation(normalizedRole),
  }
}
```

同时在 `use-applicant-detail-controller.ts` 中建立 Hook 入口，先把以下状态迁入：

```ts
const [loading, setLoading] = useState(true)
const [message, setMessage] = useState("")
const [savingProfile, setSavingProfile] = useState(false)
const [savingCase, setSavingCase] = useState(false)
const [deletingApplicant, setDeletingApplicant] = useState(false)
const [creatingCase, setCreatingCase] = useState(false)
const [detail, setDetail] = useState<ApplicantDetailResponse | null>(null)
const [basicForm, setBasicForm] = useState<BasicFormState>(emptyBasicForm)
const [selectedCaseId, setSelectedCaseId] = useState("")
const [caseForm, setCaseForm] = useState<CaseFormState>(emptyCaseForm)
const [createCaseOpen, setCreateCaseOpen] = useState(false)
const [newCaseForm, setNewCaseForm] = useState<CaseFormState>(emptyCaseForm)
```

- [ ] **步骤 4：运行测试，确认纯函数已通过**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-detail-controller.test.js
```

预期：

- 两条测试都通过

- [ ] **步骤 5：把主页面切到控制器驱动**

修改 `ApplicantDetailClientPage.tsx`，让页面壳改为读取控制器产物，而不是自己保留顶层状态与权限推导。

把原来的：

```ts
const normalizedViewerRole = useMemo(() => normalizeAppRole(viewerRole), [viewerRole])
const canEditApplicant = useMemo(() => canWriteApplicants(normalizedViewerRole), [normalizedViewerRole])
const canAssignCase = useMemo(() => canAssignCases(normalizedViewerRole), [normalizedViewerRole])
const canRunAutomation = useMemo(() => canTriggerAutomationForRole(normalizedViewerRole), [normalizedViewerRole])
const isReadOnlyViewer = useMemo(() => isReadOnlyRole(normalizedViewerRole), [normalizedViewerRole])
const [loading, setLoading] = useState(true)
const [message, setMessage] = useState("")
```

替换为：

```ts
const controller = useApplicantDetailController({
  applicantId,
  viewerRole,
})

const {
  loading,
  message,
  detail,
  basicForm,
  caseForm,
  selectedCaseId,
  createCaseOpen,
  newCaseForm,
  preview,
  auditDialog,
  capabilities,
  actions,
} = controller
```

- [ ] **步骤 6：运行构建验证**

运行：

```powershell
npm run lint
npm run build
```

预期：

- `lint` 通过
- `build` 通过

- [ ] **步骤 7：提交这一阶段**

```powershell
git add app/applicants/[id]/ApplicantDetailClientPage.tsx app/applicants/[id]/detail/types.ts app/applicants/[id]/detail/use-applicant-detail-controller.ts tests/applicant-detail-controller.test.ts
git commit -m "refactor: extract applicant detail controller"
```

### 任务 2：抽离材料预览与审核弹窗

**文件：**
- 新建：`app/applicants/[id]/detail/material-preview-dialog.tsx`
- 新建：`app/applicants/[id]/detail/audit-dialog.tsx`
- 新建：`tests/applicant-material-preview.test.ts`
- 修改：`app/applicants/[id]/ApplicantDetailClientPage.tsx`
- 修改：`app/applicants/[id]/detail/use-applicant-detail-controller.ts`

- [ ] **步骤 1：先写预览判定纯函数的失败测试**

在 `tests/applicant-material-preview.test.ts` 中先为文件预览模式判定和 Excel 首个活动 Sheet 选择写测试。

```ts
import test from "node:test"
import assert from "node:assert/strict"

import {
  getInitialExcelSheetName,
  resolveApplicantPreviewMode,
} from "../app/applicants/[id]/detail/material-preview-dialog"

test("resolveApplicantPreviewMode 识别 Word、Excel 和图片", () => {
  assert.equal(resolveApplicantPreviewMode("passport.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"), "word")
  assert.equal(resolveApplicantPreviewMode("form.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "excel")
  assert.equal(resolveApplicantPreviewMode("face.png", "image/png"), "image")
  assert.equal(resolveApplicantPreviewMode("note.txt", "text/plain"), "text")
})

test("getInitialExcelSheetName 优先返回 Sheet1", () => {
  assert.equal(getInitialExcelSheetName(["Info", "Sheet1", "Sheet2"], true), "Sheet1")
  assert.equal(getInitialExcelSheetName(["Info", "Sheet2"], true), "Info")
  assert.equal(getInitialExcelSheetName(["Main", "Extra"], false), "Main")
})
```

- [ ] **步骤 2：运行测试，确认当前失败**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-material-preview.test.js
```

预期：

- 测试失败，提示相关函数不存在

- [ ] **步骤 3：实现预览弹窗和审核弹窗组件**

在 `material-preview-dialog.tsx` 中落以下纯函数和组件壳：

```ts
export type ApplicantPreviewMode = "excel" | "word" | "image" | "text" | "html" | "unsupported"

export function resolveApplicantPreviewMode(filename: string, mime: string): ApplicantPreviewMode {
  const lowerName = filename.toLowerCase()
  const lowerMime = mime.toLowerCase()
  if (/\.(xlsx|xls)$/.test(lowerName) || lowerMime.includes("spreadsheet") || lowerMime.includes("excel")) return "excel"
  if (/\.(docx|doc)$/.test(lowerName) || lowerMime.includes("wordprocessingml")) return "word"
  if (lowerMime.startsWith("image/")) return "image"
  if (lowerMime.includes("html")) return "html"
  if (lowerMime.startsWith("text/")) return "text"
  return "unsupported"
}

export function getInitialExcelSheetName(sheetNames: string[], preferUsVisaSheet: boolean) {
  if (!sheetNames.length) return ""
  if (!preferUsVisaSheet) return sheetNames[0] || ""
  return sheetNames.find((name) => /^sheet1$/i.test(name)) || sheetNames[0] || ""
}
```

然后把原主文件里以下逻辑整体迁出：

- `selectExcelSheet`
- `setExcelCell`
- `cancelExcelEdit`
- `saveExcelChanges`
- `openFilePreview`
- 预览 Dialog JSX
- 审核 Dialog JSX

并让主页面只保留：

```tsx
<MaterialPreviewDialog
  preview={preview}
  canRunAutomation={capabilities.canRunAutomation}
  onClose={actions.closePreview}
  onSelectExcelSheet={actions.selectExcelSheet}
  onSetExcelCell={actions.setExcelCell}
  onCancelExcelEdit={actions.cancelExcelEdit}
  onSaveExcelChanges={actions.saveExcelChanges}
/>

<AuditDialog
  auditDialog={auditDialog}
  canRunAutomation={capabilities.canRunAutomation}
  onClose={actions.closeAuditDialog}
  onAutoFix={actions.autoFixUsVisaAuditIssues}
/>
```

- [ ] **步骤 4：运行测试，确认预览纯函数通过**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-material-preview.test.js
```

预期：

- 两条测试通过

- [ ] **步骤 5：运行构建验证**

运行：

```powershell
npm run lint
npm run build
```

预期：

- `lint` 通过
- `build` 通过

- [ ] **步骤 6：提交这一阶段**

```powershell
git add app/applicants/[id]/ApplicantDetailClientPage.tsx app/applicants/[id]/detail/material-preview-dialog.tsx app/applicants/[id]/detail/audit-dialog.tsx app/applicants/[id]/detail/use-applicant-detail-controller.ts tests/applicant-material-preview.test.ts
git commit -m "refactor: extract applicant material dialogs"
```

### 任务 3：抽离 Case 工作区

**文件：**
- 新建：`app/applicants/[id]/detail/cases-tab.tsx`
- 新建：`tests/applicant-cases-tab.test.ts`
- 修改：`app/applicants/[id]/ApplicantDetailClientPage.tsx`
- 修改：`app/applicants/[id]/detail/use-applicant-detail-controller.ts`

- [ ] **步骤 1：先写 Case 选择与 TLS 来源推导的失败测试**

在 `tests/applicant-cases-tab.test.ts` 中先锁定两个纯函数：当前选中法国签 Case 推导、TLS 账号信息来源推导。

```ts
import test from "node:test"
import assert from "node:assert/strict"

import {
  resolveSelectedFranceCase,
  resolveTlsAccountCaseSource,
} from "../app/applicants/[id]/detail/cases-tab"

test("resolveSelectedFranceCase 优先选当前选中且为法国签的 case", () => {
  const cases = [
    { id: "a", caseType: "us-niv", isActive: false },
    { id: "b", caseType: "france-schengen", isActive: true },
  ]
  assert.equal(resolveSelectedFranceCase(cases, cases[1])?.id, "b")
})

test("resolveTlsAccountCaseSource 在当前选中法国签 case 时优先取表单值", () => {
  const selectedCase = { caseType: "france-schengen" }
  const selectedFranceCase = { bookingWindow: "2026-05-01~2026-05-10", acceptVip: "yes", tlsCity: "paris" }
  const caseForm = { bookingWindow: "2026-06-01~2026-06-10", acceptVip: "no", tlsCity: "lyon" }
  assert.deepEqual(resolveTlsAccountCaseSource(selectedCase, selectedFranceCase, caseForm), {
    bookingWindow: "2026-06-01~2026-06-10",
    acceptVip: "no",
    tlsCity: "lyon",
  })
})
```

- [ ] **步骤 2：运行测试，确认当前失败**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-cases-tab.test.js
```

预期：

- 测试失败，提示相关函数未导出

- [ ] **步骤 3：实现 Case 工作区组件和纯函数**

在 `cases-tab.tsx` 中先把主文件已有的 `selectedFranceCase` 和 `tlsAccountCaseSource` 逻辑抽成纯函数：

```ts
export function resolveSelectedFranceCase(cases: VisaCaseRecord[], selectedCase: VisaCaseRecord | null) {
  if (selectedCase && isFranceSchengenCase(selectedCase)) return selectedCase
  return cases.find((item) => item.isActive && isFranceSchengenCase(item))
    ?? cases.find((item) => isFranceSchengenCase(item))
    ?? null
}

export function resolveTlsAccountCaseSource(
  selectedCase: VisaCaseRecord | null,
  selectedFranceCase: VisaCaseRecord | null,
  caseForm: CaseFormState,
) {
  if (selectedCase && isFranceSchengenCase(selectedCase)) {
    return {
      bookingWindow: caseForm.bookingWindow,
      acceptVip: caseForm.acceptVip,
      tlsCity: caseForm.tlsCity,
    }
  }
  if (!selectedFranceCase) return null
  return {
    bookingWindow: selectedFranceCase.bookingWindow || "",
    acceptVip: selectedFranceCase.acceptVip || "",
    tlsCity: selectedFranceCase.tlsCity || "",
  }
}
```

然后把主文件里 Case tab 对应的 JSX 从：

- `TabsContent value="cases"` 整块
- 新建 Case `Dialog` 整块

迁到 `cases-tab.tsx`，让主文件只保留：

```tsx
<TabsContent value="cases" className="space-y-6">
  <CasesTab
    detail={detail}
    selectedCaseId={selectedCaseId}
    caseForm={caseForm}
    newCaseForm={newCaseForm}
    createCaseOpen={createCaseOpen}
    canAssignCase={capabilities.canAssignCase}
    canEditApplicant={capabilities.canEditApplicant}
    savingCase={savingCase}
    creatingCase={creatingCase}
    onSelectCase={actions.selectCase}
    onCaseFormChange={actions.setCaseForm}
    onNewCaseFormChange={actions.setNewCaseForm}
    onSaveCase={actions.saveCase}
    onCreateCase={actions.createCase}
    onOpenCreateCase={actions.openCreateCase}
    onCloseCreateCase={actions.closeCreateCase}
  />
</TabsContent>
```

- [ ] **步骤 4：运行测试，确认 Case 纯函数通过**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-cases-tab.test.js
```

预期：

- 两条测试通过

- [ ] **步骤 5：运行构建验证**

运行：

```powershell
npm run lint
npm run build
```

预期：

- `lint` 通过
- `build` 通过

- [ ] **步骤 6：提交这一阶段**

```powershell
git add app/applicants/[id]/ApplicantDetailClientPage.tsx app/applicants/[id]/detail/cases-tab.tsx app/applicants/[id]/detail/use-applicant-detail-controller.ts tests/applicant-cases-tab.test.ts
git commit -m "refactor: extract applicant cases workspace"
```

### 任务 4：抽离基础 tab、材料 tab、进度 tab，并把主文件收成页面壳

**文件：**
- 新建：`app/applicants/[id]/detail/basic-tab.tsx`
- 新建：`app/applicants/[id]/detail/materials-tab.tsx`
- 新建：`app/applicants/[id]/detail/progress-tab.tsx`
- 修改：`app/applicants/[id]/ApplicantDetailClientPage.tsx`
- 修改：`app/applicants/[id]/detail/types.ts`
- 修改：`app/applicants/[id]/detail/use-applicant-detail-controller.ts`

- [ ] **步骤 1：先写页面壳 tab 解析的失败测试补充**

把任务 1 的控制器测试补充一条，锁定页面壳只认四个 tab，并且最终落到 `TabsContent` 的分发关系不变。

```ts
test("resolveApplicantDetailTab 对非法值始终回退到 basic", () => {
  assert.equal(resolveApplicantDetailTab(undefined), "basic")
  assert.equal(resolveApplicantDetailTab(null), "basic")
  assert.equal(resolveApplicantDetailTab("timeline"), "basic")
})
```

- [ ] **步骤 2：运行测试，确认补充测试当前通过或按需要修正**

运行：

```powershell
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-detail-controller.test.js
```

预期：

- tab 解析测试全部通过

- [ ] **步骤 3：抽出三个 tab 子组件**

把主文件里以下 `TabsContent` 内容分别迁出：

- `value="basic"` 迁到 `basic-tab.tsx`
- `value="materials"` 迁到 `materials-tab.tsx`
- `value="progress"` 迁到 `progress-tab.tsx`

主文件中的 tab 组装收成：

```tsx
<Tabs key={activeTab} defaultValue={activeTab} className="space-y-5">
  <TabsList className="grid h-auto w-full grid-cols-4 rounded-2xl border border-slate-200 bg-white/90 p-1.5 shadow-sm backdrop-blur">
    <TabsTrigger value="basic">基础资料</TabsTrigger>
    <TabsTrigger value="cases">Case</TabsTrigger>
    <TabsTrigger value="materials">材料</TabsTrigger>
    <TabsTrigger value="progress">进度</TabsTrigger>
  </TabsList>

  <BasicTab {...controller.basicTabProps} />
  <CasesTab {...controller.casesTabProps} />
  <MaterialsTab {...controller.materialsTabProps} />
  <ProgressTab {...controller.progressTabProps} />
</Tabs>
```

这一步结束后，`ApplicantDetailClientPage.tsx` 只应保留：

- 路由相关 Hook
- 控制器调用
- tab 组装
- 弹窗挂载
- 少量页面级布局 JSX

- [ ] **步骤 4：运行完整验证**

运行：

```powershell
npm run lint
npm run build
npx tsc -p tsconfig.test.json
node --test .test-dist/tests/applicant-detail-controller.test.js
node --test .test-dist/tests/applicant-material-preview.test.js
node --test .test-dist/tests/applicant-cases-tab.test.js
```

预期：

- 所有命令通过
- `/applicants/[id]` 页面仍能打开并保持原行为

- [ ] **步骤 5：提交这一阶段**

```powershell
git add app/applicants/[id]/ApplicantDetailClientPage.tsx app/applicants/[id]/detail/basic-tab.tsx app/applicants/[id]/detail/materials-tab.tsx app/applicants/[id]/detail/progress-tab.tsx app/applicants/[id]/detail/types.ts app/applicants/[id]/detail/use-applicant-detail-controller.ts tests/applicant-detail-controller.test.ts
git commit -m "refactor: turn applicant detail page into shell"
```

## 自查

### 规格覆盖

本计划已经覆盖 spec 中的四个实施阶段：

- 先抽控制器
- 再抽预览与审核弹窗
- 再抽 Case 工作区
- 最后抽剩余 tab 并把页面壳收口

### 占位符检查

本计划没有使用 `TODO`、`TBD`、`implement later` 之类占位描述，所有步骤都给出了具体文件和命令。

### 类型与命名一致性

计划中统一使用以下命名：

- `resolveApplicantDetailTab`
- `buildApplicantDetailCapabilities`
- `useApplicantDetailController`
- `MaterialPreviewDialog`
- `AuditDialog`
- `CasesTab`

后续实施时，不应再引入第二套近似命名，避免控制器与子组件接口漂移。

